<?xml version="1.0" encoding="utf-8"?>
<!-- GOOD: data_extraction_rules.xml (Android 12+ / API 31+)
     From the article section "Backup Configuration Leaks". -->
<data-extraction-rules>
  <cloud-backup>
    <!-- Exclude sensitive files from Google cloud backup -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="sensitive/" />
  </cloud-backup>
  <device-transfer>
    <!-- Also exclude from device-to-device transfer -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
  </device-transfer>
</data-extraction-rules>